<Page x:Class="SheduleCreator.Pages.ClassSchedulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SheduleCreator.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClassSchedulePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка "Назад" -->
        <Button Content="Назад" Click="Back_Click" HorizontalAlignment="Left" Margin="10" Padding="10"/>

        <!-- Таблицы для каждого класса -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Класс 1 -->
                <TextBlock Text="Класс 1" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class1Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 2 -->
                <TextBlock Text="Класс 2" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class2Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Повторите для остальных классов (3-9) -->
                <!-- Класс 3 -->
                <TextBlock Text="Класс 3" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class3Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 4 -->
                <TextBlock Text="Класс 4" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class4Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 5 -->
                <TextBlock Text="Класс 5" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class5Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 6 -->
                <TextBlock Text="Класс 6" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class6Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 7 -->
                <TextBlock Text="Класс 7" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class7Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 8 -->
                <TextBlock Text="Класс 8" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class8Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Класс 9 -->
                <TextBlock Text="Класс 9" FontWeight="Bold" Margin="10,10,10,5"/>
                <DataGrid x:Name="Class9Grid" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="Часов в неделю" Binding="{Binding HoursPerWeek}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
