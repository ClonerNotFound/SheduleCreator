<Page x:Class="SheduleCreator.Pages.ViewSubjectsTeachersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SheduleCreator.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewSubjectsTeachersPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="Back" Content="Назад" Click="Back_Click"  Margin="5" Padding="10"/>
            <Button x:Name="Refresh" Content="Обновить" Click="Refresh_Click" Margin="5" Padding="10"/>
            <Button x:Name="Import" Content="Импорт в JSON" Click="Import_Click"  Margin="5" Padding="10"/>
            <Button x:Name="Export" Content="Экспорт в JSON" Click="Export_Click" Margin="5" Padding="10"/>
        </StackPanel>
        <DataGrid x:Name="SubjectsDataGrid" Grid.Row="1" AutoGenerateColumns="True" Margin="10"/>
    </Grid>
</Page>
